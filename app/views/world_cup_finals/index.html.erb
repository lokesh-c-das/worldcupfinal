<h1>World Cup Finals</h1>

<%= link_to 'New World Cup', new_world_cup_final_path %>
<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Year</th>
      <th scope="col">Champion</th>
      <th scope="col">Score</th>
      <th scope="col">Runnerup</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
  <% finals.each do |final|%>
    <tr>
      <th scope="row"> <%= final.year%> </th>
      <td><%= final.champion%></td>
      <td><%= final.champion_score%>-<%= final.runner_up_score%></td>
      <td><%= final.runner_up%></td>
      <td> 
      <%= link_to '🔎', world_cup_final_path(final)%>
       <%= link_to '🖋', edit_world_cup_final_path(final) %> <%= link_to '🗑', world_cup_final_path(final), method: :delete %></td>
       <i class="fas fa-cat"></i>
    </tr>
    <% end %>
  </tbody>
</table>


